# JustVoxel

JustVoxel is a purpose-built immutable Minecraft server appliance for the Home Server Project.

Development is staged on the `testing` branch. The `main` branch is reserved for validated promotions and stable releases.

> Project bootstrap only. The implementation is currently under development; do not use this branch as a production image yet.
